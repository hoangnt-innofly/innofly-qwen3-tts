-r requirements.txt

# Same CUDA 12 setup as the working LTX server (cu121).
# torch and torchaudio MUST come from that index. `pip install qwen-tts`
# can pull PyPI torchaudio (CUDA 13 / libcudart.so.13) — reinstall after.
#
#   python -m pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
#   python -m pip install -U qwen-tts
#   python -m pip install --force-reinstall --no-deps torch torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# Optional, if the GPU supports it:
#   pip install -U flash-attn --no-build-isolation
#
# Linux (apt, not pip) — Triton compiles driver.c on first CUDA run:
#   sudo apt-get install -y sox libsox-fmt-all build-essential python3.11-dev

qwen-tts
